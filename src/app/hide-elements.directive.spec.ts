import { HideElementsDirective } from './hide-elements.directive';

describe('HideElementsDirective', () => {
  it('should create an instance', () => {
    const directive = new HideElementsDirective();
    expect(directive).toBeTruthy();
  });
});
